# Copy this file to .env and fill in real values.
# Vite only exposes vars prefixed VITE_ to client code — restart the dev
# server after editing this file, it's only read at startup.

VITE_API_KEY=
VITE_API_URL=http://localhost:8000
